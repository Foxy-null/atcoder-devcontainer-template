#!/usr/bin/env bash
set -euo pipefail

source /opt/atcoder/gcc/share/atcoder/flags.sh
# Libraries follow the source/object files; caller options can override -O2 for GDB.
exec /opt/atcoder/gcc/bin/g++ "${COMPILE_FLAGS[@]}" "$@" "${LINK_FLAGS[@]}"
